# V23 — แก้เฉพาะการตั้งค่ากล่อง Admin

แก้จาก V22 เฉพาะส่วน `box_settings`:
- เปลี่ยนชื่อ/ชื่ออังกฤษ/ราคา/ระดับกล่องได้
- เปลี่ยนชื่อรางวัล/ระดับ/อัตราดรอปได้ โดยไม่บังคับยอดรวมต้องเท่ากับ 100% (เกมสุ่มแบบ weighted ตามค่าที่ตั้ง)
- เพิ่มและลบรางวัลได้
- เพิ่ม/เปลี่ยนรูปได้
- อัปโหลดรูปไป Supabase Storage ก่อน ถ้า Storage policy/bucket ใช้งานไม่ได้ จะบีบอัดรูปและเก็บเป็น data URL ใน `box_settings` เพื่อไม่ให้การบันทึกติดที่ระบบ Storage
- กดบันทึก -> ยืนยัน -> เขียนฐานข้อมูล -> อ่านกลับมาตรวจ -> รีเฟรชหน้า Admin
- ถ้าบันทึกหรือ verify ไม่ผ่าน จะไม่รีเฟรชและจะแสดง error

## SQL
รัน `BOX_SETTINGS_SAVE_V23.sql` ใน Supabase SQL Editor 1 ครั้ง แล้ว Deploy เว็บชุดนี้

ไม่ต้องรัน SQL ตัวอื่นเพื่อแก้ปัญหานี้
