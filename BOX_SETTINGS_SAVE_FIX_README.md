# แก้ระบบบันทึกกล่อง/ของรางวัล — V18 Fix

แก้เฉพาะจุดระบบ `📦 จัดการกล่อง` ฝั่ง Admin:

- กด `บันทึกการตั้งค่ากล่องทั้งหมด` แล้วมีหน้าต่างยืนยันก่อนบันทึก
- ยกเลิก = ไม่บันทึกและไม่เปลี่ยนข้อมูล
- ยืนยัน = บันทึกชื่อกล่อง ราคา ระดับ รางวัล อัตราดรอป และ URL รูปภาพ
- ใช้ `admin_save_box_settings_v2` แบบ `SECURITY DEFINER` เป็นทางหลัก เพื่อไม่ติดปัญหา RLS ของตาราง
- หลังบันทึกจะโหลดค่าจาก Supabase กลับมาอีกครั้ง เพื่อยืนยันว่าค่าที่บันทึกแล้วถูกต้อง
- หน้าเกมอ่าน `box_settings` จาก Supabase อยู่แล้ว จึงนำค่าที่บันทึกไปใช้จริง

## ต้องรันใน Supabase 1 ครั้ง
รันไฟล์ `BOX_SETTINGS_SAVE_CONFIRM_FIX.sql` ทั้งไฟล์ใน SQL Editor แล้ว Deploy ชุดเว็บนี้ขึ้น Render จากนั้นกด Ctrl+F5 ที่หน้า Admin และหน้าเกม

**ไม่ได้แก้ระบบอื่น** เช่น เติมเงิน, ผู้เล่น, ขอรับรางวัล, Auth หรือคลังรางวัล
