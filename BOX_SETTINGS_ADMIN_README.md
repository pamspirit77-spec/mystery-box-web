# ระบบจัดการกล่องสุ่ม — Admin

ไฟล์ที่เพิ่ม/แก้เฉพาะระบบกล่องสุ่ม:
- `BOX_SETTINGS_SETUP.sql` — สร้างตารางตั้งค่ากล่อง + Storage สำหรับรูป + RPC ฝั่ง Admin
- `admin.html` / `admin.js` / `admin.css` — เพิ่มแท็บ `📦 จัดการกล่อง`
- `app.js` / `online.js` / `styles.css` — ให้หน้าเกมอ่านค่ากล่องจาก Supabase, สุ่มตาม drop rate และแสดงรูปที่ Admin อัปโหลด

## ต้องทำใน Supabase 1 ครั้ง
เปิด SQL Editor แล้วรันไฟล์ `BOX_SETTINGS_SETUP.sql` ทั้งไฟล์

จากนั้นรีเฟรชเว็บ Admin จะมีแท็บ **📦 จัดการกล่อง**

## สิ่งที่ Admin ปรับได้
- กล่องทั้ง 5 กล่อง
- ชื่อกล่อง
- ราคาเปิดกล่อง
- เพิ่ม/ลบรางวัล
- ชื่อรางวัล
- ระดับรางวัล
- อัตราดรอปของแต่ละรางวัล (ต้องรวม 100%)
- แนบรูปภาพของแต่ละรางวัล

รูปที่อัปโหลดจะถูกเก็บใน Supabase Storage และหน้าเกมจะนำ URL รูปนั้นมาแสดงตอนสุ่ม/เปิดกล่อง รวมถึงรายการรางวัลและคลังรางวัล

ระบบอื่น เช่น เติมเงิน, ผู้เล่น, ขอรับรางวัล, Auth และตั้งค่าเว็บ ไม่ได้เปลี่ยน logic เดิม
